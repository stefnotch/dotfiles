# DuckDNS

Copy your [DuckDNS token](https://www.duckdns.org/install.jsp) to `/etc/duckdns.key`.

Then this thing should work.

## Extra domains

Hardcode these domains to have the homeserver's IP address.
- `stefnotch-home.duckdns.org`
